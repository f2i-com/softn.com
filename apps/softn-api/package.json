{
  "name": "@softn/api",
  "version": "0.0.1",
  "private": true,
  "description": "The softn.com directory API: PHP and SQLite, deployed as /api/ beside the static site",
  "type": "module",
  "scripts": {
    "test": "node --test test/api.test.mjs"
  }
}
