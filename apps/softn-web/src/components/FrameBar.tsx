/** Lives in @softn/runtime-shell; re-exported here so the old path keeps working. */
export { FrameBar } from '@softn/runtime-shell/FrameBar';
export type { TabInfo } from '@softn/runtime-shell/FrameBar';
