{
  "name": "@softn/runtime-shell",
  "version": "0.0.12",
  "private": true,
  "description": "What every host that runs one .softn bundle in a browser shares: reading and composing the bundle (bundleProcessor), warming its first screen off the main thread (zipWarmup + zipWorker) and the slim frame bar drawn over an app. The web runtime and the single-app shell both build on it; neither owns it.",
  "license": "Apache-2.0",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./bundleProcessor": "./src/bundleProcessor.ts",
    "./zipWarmup": "./src/zipWarmup.ts",
    "./FrameBar": "./src/FrameBar.tsx",
    "./package.json": "./package.json"
  },
  "scripts": {
    "test": "vitest run"
  },
  "dependencies": {
    "@softn/brand": "*",
    "@softn/core": "*"
  },
  "peerDependencies": {
    "react": "^18.0.0 || ^19.0.0"
  },
  "devDependencies": {
    "@types/react": "^18.2.0",
    "fflate": "^0.8.2",
    "jsdom": "^24.0.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "typescript": "^5.3.0",
    "vitest": "^4.1.11"
  }
}
