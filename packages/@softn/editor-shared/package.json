{
  "name": "@softn/editor-shared",
  "version": "0.0.13",
  "private": true,
  "description": "What Builder and Studio share and neither owns: the hand-off to the runtime and publish page, opening a bundle from a same-origin link, modal keyboard ownership, and the private FormLogic editor channel.",
  "license": "Apache-2.0",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./handoff": "./src/handoff.ts",
    "./remoteOpen": "./src/remoteOpen.ts",
    "./useModalFocus": "./src/useModalFocus.ts",
    "./hostedEditor": "./src/hostedEditor.ts",
    "./package.json": "./package.json"
  },
  "scripts": {
    "test": "vitest run"
  },
  "dependencies": {
    "@softn/bundle-format": "*"
  },
  "peerDependencies": {
    "react": "^18.0.0 || ^19.0.0"
  },
  "devDependencies": {
    "@types/react": "^18.2.0",
    "jsdom": "^24.0.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "typescript": "^5.3.0",
    "vitest": "^4.1.11"
  }
}
