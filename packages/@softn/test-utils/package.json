{
  "name": "@softn/test-utils",
  "version": "0.0.18",
  "private": true,
  "description": "Test doubles more than one workspace needs. Nothing here ships; a workspace lists it under devDependencies and imports it by name instead of reaching into another workspace's test folder.",
  "license": "Apache-2.0",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./fake-indexeddb": "./src/fake-indexeddb.ts",
    "./package.json": "./package.json"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}
