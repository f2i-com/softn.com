/**
 * The theme on its own: ThemeProvider, the token sets, the variant and size
 * scales. A host shell wraps its chrome in the provider before any app has
 * been chosen, so this entry must not pull a single component in with it.
 */
export * from '../theme';
