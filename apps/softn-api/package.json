{
  "name": "@softn/api",
  "version": "0.0.15",
  "private": true,
  "description": "The softn.com directory API: PHP with folder discovery and atomic JSON metadata",
  "type": "module",
  "scripts": {
    "test": "node --test test/*.test.mjs"
  }
}
