/**
 * The permission declarations the directory describes, from the shared
 * bundle contract (@softn/bundle-format). This used to be a copy of core's
 * module, kept because the site must not depend on the engine; the contract
 * package carries no engine, so the copy is gone.
 */
export * from '@softn/bundle-format/capabilities';
