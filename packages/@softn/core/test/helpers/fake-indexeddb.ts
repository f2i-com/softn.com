/**
 * The fake lives in @softn/test-utils now, where the site's and Studio's
 * tests take it from by name; this path stays so core's own tests read as
 * they did.
 */
export * from '@softn/test-utils/fake-indexeddb';
