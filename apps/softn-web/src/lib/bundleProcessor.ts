/**
 * Lives in @softn/runtime-shell; re-exported here so every import and test
 * that named this path keeps working. `extractPermissions` comes from core
 * through the package, as before.
 */
export * from '@softn/runtime-shell/bundleProcessor';
