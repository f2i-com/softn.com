/**
 * The syntax scanners on their own: `tokenize` and friends, no React and no
 * component. Studio's read-only code view renders from this without pulling
 * the editors in with it.
 */
export * from '../editors/highlight';
